#!/bin/bash
# OpenClaw Launcher - Activates venv and starts CLI

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Activate virtual environment
source "$SCRIPT_DIR/.venv/bin/activate"

# Run CLI
python -m src.cli "$@"
